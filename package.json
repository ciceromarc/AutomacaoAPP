{
  "name": "projeto-api-cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cypress:open": "./node_modules/.bin/cypress open",
    "cypress:run": "./node_modules/.bin/cypress run --headless",
    "cypress:run:ci": "./node_modules/.bin/cypress run --headless --browser chrome --config video=true,screenshotOnRunFailure=true",
    "cypress:debug": "./node_modules/.bin/cypress run --headless --browser chrome --config video=true,screenshotOnRunFailure=true --reporter spec",
    "e2e": "cypress run --env allure=true",
    "report:allure": "allure generate allure-results --clean -o allure-report && allure open  allure-report",
    "report:allure:ci": "allure generate allure-results --clean -o allure-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.41.2",
    "allure-commandline": "^2.34.1",
    "cypress": "^14.5.2"
  },
  "dependencies": {
    "cypress-plugin-api": "^2.11.2"
  }
}
